#include <stdio.h>

int main() {
    float ax,ay,bx,by;
    printf("Add meg Ax komponenset:");
    scanf("%f",&ax);
    printf("Add meg Ay komponenset: ");
    scanf("%f",&ay);
    printf("Add meg Bx komponenset:");
    scanf("%f",&bx);
    printf("Add meg By")
    return 0;
}
